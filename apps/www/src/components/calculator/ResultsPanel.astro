<div class="space-y-6">
  <!-- Sticky container for results -->
  <div class="md:sticky md:top-24 space-y-6">
    <!-- Total Monthly Impact -->
    <div class="card bg-accent-success-subtle border-2 border-accent-success/20">
      <!-- Profit Increase - Hero Section -->
      <div class="text-center mb-6">
        <p class="text-sm font-medium text-accent-success mb-3" id="profitLabel">
          Annual Profit Increase
        </p>
        <p class="text-5xl font-bold text-accent-success mb-2" id="displayValue">$0</p>
        <div class="flex items-center justify-center gap-3 text-accent-success">
          <span class="text-2xl font-bold" id="marginExpansion">0%</span>
          <span class="text-sm font-medium">margin expansion</span>
        </div>
      </div>

      <!-- Supporting Metrics -->
      <div class="pt-6 border-t border-accent-success/20 space-y-2">
        <div class="flex justify-between items-center text-sm">
          <span class="text-secondary" id="baselineLabel">Baseline Annual Net Sales</span>
          <span class="font-semibold text-primary" id="baselineRevenue">$0</span>
        </div>
      </div>
    </div>

    <!-- Value Breakdown -->
    <div class="card">
      <div class="flex items-center gap-2 mb-4">
        <h3 class="text-lg font-bold text-primary">Value Breakdown</h3>
      </div>

      <div class="space-y-4">
        <!-- Exchange Value -->
        <div class="flex items-start justify-between pb-4 border-b border-subtle">
          <div>
            <p class="font-medium text-primary">Exchange Value</p>
            <p class="text-sm text-tertiary mt-1">Returns converted to exchanges</p>
          </div>
          <p class="text-lg font-semibold text-accent-success" id="exchangeValue">$0</p>
        </div>

        <!-- Keep Item Value -->
        <div class="flex items-start justify-between pb-4 border-b border-subtle">
          <div>
            <p class="font-medium text-primary">Keep Item Value</p>
            <p class="text-sm text-tertiary mt-1">Returns avoided entirely</p>
          </div>
          <p class="text-lg font-semibold text-accent-success" id="keepValue">$0</p>
        </div>

        <!-- Retention Value -->
        <div class="flex items-start justify-between">
          <div>
            <p class="font-medium text-primary">Retention Value</p>
            <p class="text-sm text-tertiary mt-1">Increased customer lifetime value</p>
          </div>
          <p class="text-lg font-semibold text-accent-success" id="retentionValue">$0</p>
        </div>
      </div>
    </div>

    <!-- Impact Metrics -->
    <div class="card">
      <h3 class="text-lg font-bold text-primary mb-4">Other Metrics</h3>

      <div class="space-y-3">
        <div class="flex justify-between">
          <span class="text-sm text-secondary">Eligible Returns</span>
          <span class="text-sm font-medium text-primary" id="eligibleReturns">0</span>
        </div>
        <div class="flex justify-between">
          <span class="text-sm text-secondary">Engaged Customers</span>
          <span class="text-sm font-medium text-primary" id="engagedCustomers">0</span>
        </div>
        <div class="flex justify-between">
          <span class="text-sm text-secondary">Returns Prevented</span>
          <span class="text-sm font-medium text-primary" id="returnsPrevented">0</span>
        </div>
      </div>
    </div>

    <!-- CTA -->
    <div class="card border-2 border-accent-primary/20">
      <div class="text-center">
        <p class="text-sm text-secondary mb-4">Ready to see these results for your business?</p>
        <a
          href="https://calendly.com/alejandro-materialmodel/return-signals-introductory-call"
          target="_blank"
          rel="noopener noreferrer"
          class="btn btn-primary w-full"
        >
          Book a Demo
        </a>
      </div>
    </div>
  </div>
</div>
