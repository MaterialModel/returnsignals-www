---
/**
 * CTASection
 *
 * Section 10: "Looking for a few design partners"
 * Final call-to-action with external form link
 */

interface Props {
  ctaUrl: string
  className?: string
}

const { ctaUrl, className = '' } = Astro.props
---

<section class={`bg-surface-base py-20 sm:py-32 ${className}`}>
  <div class="container-narrow">
    <div class="text-center max-w-2xl mx-auto">
      <h2 class="section-title mb-6 animate-on-scroll">Looking for a few design partners.</h2>

      <p class="section-description mb-10 animate-on-scroll">
        If you own CX, returns, or operations and want to reduce avoidable returns while improving
        post-purchase experience, we'd like to learn how you handle this today and share what we're
        building.
      </p>

      <div class="animate-on-scroll">
        <a
          href={ctaUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="btn btn-primary btn-lg"
        >
          Request pilot
        </a>
      </div>

      <p class="text-sm text-tertiary mt-6 animate-on-scroll">
        No spam. We'll only reach out about pilots / product updates.
      </p>
    </div>
  </div>
</section>
